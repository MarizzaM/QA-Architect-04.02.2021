1 -- The meaning of Encapsulation, is to make sure that "sensitive" data is hidden from users.

2 -- Access Modifiers - controls the access level

3 -- Increased security of data

4 --  The hierarchical file structure similarly directory structure on PC